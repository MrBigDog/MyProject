SET(TARGET_SRC voxel_grid.cpp)
				
SET(TARGET_H)

INCLUDE_DIRECTORIES(${OSG_INCLUDE_DIR} ${PCL_INCLUDE_DIR})

SET(TARGET_LIBRARIES_VARS 
	OSG_LIBRARY 
	OSGGA_LIBRARY 
	OSGDB_LIBRARY 
	OSGUTIL_LIBRARY 
	OSGVIEWER_LIBRARY 
	OPENTHREADS_LIBRARY
	PCL_COMMON_LIBRARY
	PCL_IO_LIBRARY
	)
	
SET(EXECUTABLE_OUTPUT_PATH ${OUTPUT_LIBDIR})

SETUP_APPLICATION(VoxelTest3)