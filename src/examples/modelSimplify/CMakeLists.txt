
SET(TARGET_H    )
SET(TARGET_SRC  main.cpp)

INCLUDE_DIRECTORIES(${GWEARTH_SOURCE_DIR}/Src  ${OSG_INCLUDE_DIR}  )

SET(TARGET_LIBRARIES_VARS 
	OSG_LIBRARY 
	OSGGA_LIBRARY 
	OSGDB_LIBRARY 
	OSGUTIL_LIBRARY 
	OSGVIEWER_LIBRARY 
	OPENTHREADS_LIBRARY
	#OSGEARTH_LIBRARY
	#OSGEARTHUTIL_LIBRARY
	#OSGEARTHSYMBOLOGY_LIBRARY
	#LIBMONGOCXX_LIBRARIES
	)
	
#LINK_INTERNAL(${LIB_NAME} gwEarth gwUtil)

#SET(TARGET_COMMON_LIBRARIES gwEarth gwApp )
	
SET(EXECUTABLE_OUTPUT_PATH ${OUTPUT_LIBDIR})

SETUP_APPLICATION(modelSimplify)