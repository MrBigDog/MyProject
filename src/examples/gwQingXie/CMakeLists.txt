SET(TARGET_H StringUtils.h FileUtils.h)
SET(TARGET_SRC  main.cpp StringUtils.cpp FileUtils.cpp)

INCLUDE_DIRECTORIES(${OSG_INCLUDE_DIR})

SET(TARGET_LIBRARIES_VARS 
	OSG_LIBRARY 
	OSGDB_LIBRARY 
	OPENTHREADS_LIBRARY
	#OSGVIEWER_LIBRARY
	)

#SET(TARGET_COMMON_LIBRARIES gwGeologicalUtil )
SET(EXECUTABLE_OUTPUT_PATH ${OUTPUT_LIBDIR})
SETUP_APPLICATION(building_format_conversion)