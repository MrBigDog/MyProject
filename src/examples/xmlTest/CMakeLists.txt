SET(TARGET_SRC xml2json.cpp test.cpp)
				
SET(TARGET_H xml2json.hpp)

INCLUDE_DIRECTORIES(${OSG_INCLUDE_DIR} ${OSGEARTH_INCLUDE_DIR})

SET(TARGET_LIBRARIES_VARS 
	OSG_LIBRARY 
	OSGGA_LIBRARY 
	OSGDB_LIBRARY 
	OSGUTIL_LIBRARY
	OSGEARTH_LIBRARY
	OSGVIEWER_LIBRARY 
	OPENTHREADS_LIBRARY
	)
	
SET(EXECUTABLE_OUTPUT_PATH ${OUTPUT_LIBDIR})

SETUP_APPLICATION(xmlTest)